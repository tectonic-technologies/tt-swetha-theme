{%- comment -%}AUTO-GENERATED — bkodjs1e demo preset. Programmatic install, not Studio-managed.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'on' -%}
{%- endif -%}

{%- assign sai_handle = 'spectrum_snippet_bkodjs1e_7lcha1xo4nteu48xhlrr9etf_live' -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    <div data-spectrum-instance-id="7lcha1xo4nteu48xhlrr9etf" data-spectrum-snippet-id="bkodjs1e" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-7lcha1xo4nteu48xhlrr9etf" data-spectrum-experience="_system-196419092640" data-spectrum-feature-slug="best_price">
      {% render '_sai-snippet-bkodjs1e', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-7lcha1xo4nteu48xhlrr9etf', experience_handle: '_system-196419092640' %}
    </div>
  {%- endif -%}
{%- endif -%}
