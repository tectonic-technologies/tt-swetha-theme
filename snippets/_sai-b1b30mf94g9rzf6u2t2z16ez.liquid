{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_h9k3pdtz_b1b30mf94g9rzf6u2t2z16ez_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_h9k3pdtz_b1b30mf94g9rzf6u2t2z16ez_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"b1b30mf94g9rzf6u2t2z16ez",
    "snippetId":"h9k3pdtz",
    "experienceId":"019e43a1-70a8-7603-9be4-d4914e109027"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    "images":{
    {%- for sai_item in sai_mo.images.value -%}
      {%- unless forloop.first -%},{%- endunless -%}
      "{{ sai_item.system.handle | default: sai_item.id }}":{
        "url":{{ sai_item | image_url: width: 2400 | json }},
        "width":{{ sai_item.width | json }},
        "height":{{ sai_item.height | json }},
        "alt":{{ sai_item.alt | json }}
      }
    {%- endfor -%}
    }
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="b1b30mf94g9rzf6u2t2z16ez">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="b1b30mf94g9rzf6u2t2z16ez" data-spectrum-snippet-id="h9k3pdtz" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-b1b30mf94g9rzf6u2t2z16ez" data-spectrum-experience="_system-196419092640" data-spectrum-feature-slug="hero_banner">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-h9k3pdtz', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-b1b30mf94g9rzf6u2t2z16ez', experience_handle: '_system-196419092640' %}
    </div>
  {%- endif -%}
{%- endif -%}