{%- comment -%}
  Demo leftover. Original Studio mutation row was deleted from the DB but
  this wrapper file was orphaned on the theme. Replaced with an empty
  no-op to suppress rendering without breaking section render calls.
{%- endcomment -%}
